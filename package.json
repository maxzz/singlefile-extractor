{
    "name": "extractor-from-singlefile",
    "version": "0.1.0",
    "description": "Extract an elelement + inline styles from SingleFile-saved HTML into a standalone HTML.",
    "private": true,
    "license": "MIT",
    "scripts": {
        "extract": "python singlefile_extractor.py",
        "extract:help": "python singlefile_extractor.py --help"
    }
}
